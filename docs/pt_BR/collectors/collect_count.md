---
title: "CollectCount"
lang: pt_BR
---

# `CollectCount`

Helper para consultas com `countOutput:true` (retorna contagens numéricas em vez de listas completas).

Uso

- Assinatura: `CollectCount(apiUrl, token, method string, params map[string]interface{}, req ApiRequester) (int, error)`
