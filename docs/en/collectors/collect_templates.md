---
title: "CollectTemplates"
lang: en_US
---

# `CollectTemplates`

Returns templates and metadata used across the report.

Usage

- `CollectTemplates(apiUrl, token, req)` — calls `template.get` and returns parsed results
